const lightbox=document.getElementById('lightbox');const lightboxImage=document.getElementById('lightbox-image');const close=()=>{lightbox.classList.remove('open');lightboxImage.src=''};
document.querySelectorAll('.gallery').forEach(gallery=>{const main=gallery.querySelector('.gallery-main img');const button=gallery.querySelector('.gallery-main .image-button');const caption=gallery.querySelector('.gallery-caption');const thumbs=[...gallery.querySelectorAll('.gallery-thumb')];thumbs.forEach((thumb,index)=>{thumb.addEventListener('click',()=>{const src=thumb.dataset.src;main.src=src;button.dataset.lightbox=src;caption.innerHTML=`<span>${thumb.dataset.caption}</span><span>${String(index+1).padStart(2,'0')} / ${String(thumbs.length).padStart(2,'0')}</span>`;thumbs.forEach(t=>t.classList.remove('is-active'));thumb.classList.add('is-active')})});button.addEventListener('click',()=>{lightboxImage.src=button.dataset.lightbox;lightbox.classList.add('open')})});
document.querySelector('.lightbox-close').addEventListener('click',close);lightbox.addEventListener('click',e=>{if(e.target===lightbox)close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});
